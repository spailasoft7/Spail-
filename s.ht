<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Spailã survey</title>
       <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
        body {
    background: black;
}
.loader{
    position:absolute;
    width: 35px ;
    height: 35px;
    border:10px solid #1f51ff;
    border-right-color: #40cbff;
    border-radius:50%;
    animation-name: loader ;
    animation: loader ease-in-out infinite;
    animation-duration:5s;
    animation-delay:1;
    animation-directiom: alternate;
    
}
@keyframes loader{
    0%{
        
    }
    25%{
        transform:rotate(360deg);
    }
    50%{
        transform:rotate(-360deg);
       
    }
    100%{
        
    }
}
 
    </style>
</head>
<body style="background:black;">
    <center><<a href="index.html">back</a></center>
<br>
<br>
<br>

    <center><div style="position: relative;">
  <div style="position: absolute; top: 0; left: 0; z-index: -1;" class="loader" show>
 </div></div>
  </center> 
<iframe width="100%" frameBorder="0" height="700px" src="https://offers.cpx-research.com/index.php?app_id=31301&ext_user_id={unique_user_id}&secure_hash={secure_hash}&username={user_name}&email={user_email}&subid_1=&subid_2"></iframe> 

<script>

const script4 = {
    div_id: "notification",
    theme_style: 4,
    position: 5, //number // default 1 // 1 = top center, 2 = top left, 3 top right, 4 bottom left, 5 bottom right, 6 bottom center
    text: "New message",
    link: "https://offers.cpx-research.com/index.php?app_id={app_id}&ext_user_id={ext_user_id}&username={username}&email={email}", // replace ext_user_id app_id and email (not necessary)
    newtab: true
};



const config = {
    general_config: {
        app_id: {app_id}, //number
        ext_user_id: "{ext_user_id}", // string
				email: "{email}", // string
        username: "{username}", // string
				subid_1: "", // string
        subid_2: "", // string
    },
    style_config: {
        text_color: "#2b2b2b", // string // hex, rgba, colorcode
        survey_box: {
            topbar_background_color: "#45c4c6", // string // hex, rgba, colorcode
            box_background_color: "white", // string // hex, rgba, colorcode
            rounded_borders: false, // boolean true || false
            stars_filled: "#2b2b2b", // string // hex, rgba, colorcode
        },
    },
    script_config: [script4], // Object Array
    debug: true, // boolean      
};

window.config = config;   
    </script>
               <div id="notification"></div>

<script type="text/javascript" src="https://cdn.cpx-research.com/assets/js/script_tag_v1.1.js"></script>
</body>
</html>
